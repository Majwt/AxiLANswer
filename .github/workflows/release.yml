
name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci
        working-directory: frontend
      - run: |
          VERSION="${GITHUB_REF_NAME#v}"
          VITE_APP_VERSION=$VERSION npm run build
        working-directory: frontend

      - name: Zip frontend
        run: |
          cd frontend/dist
          zip -r ../../axilanswer-frontend.zip .

      - uses: actions/upload-artifact@v4
        with:
          name: axilanswer-frontend
          path: axilanswer-frontend.zip
          if-no-files-found: error

  build-backend:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - rid: win-x64
            name: axilanswer-api-windows
          - rid: linux-x64
            name: axilanswer-api-linux

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Publish backend
        run: |
          VERSION="${GITHUB_REF_NAME#v}"

          dotnet publish \
            api \
            -c Release \
            -r ${{ matrix.rid }} \
            --self-contained false \
            -p:Version=$VERSION \
            -p:AssemblyVersion=$VERSION.0 \
            -p:FileVersion=$VERSION.0 \
            -p:InformationalVersion=$GITHUB_REF_NAME \
            -o api/publish/${{ matrix.name }}
      - name: Zip backend
        run: |
          cd api/publish/${{ matrix.name }}
          zip -r ../${{ matrix.name }}.zip .

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: api/publish/${{ matrix.name }}.zip
          if-no-files-found: error

  release:
    needs:
      - build-frontend
      - build-backend

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir release
          find artifacts -name "*.zip" -exec cp {} release/ \;

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.zip
